## Modelo do banco de dados

user = {
    id => hash_key
    name
    followers_count
}

total_by_hours = {
    created_at => hash_key
    hastag
}

hashtag_by_country {
    hastang => hash_key
    country
}
